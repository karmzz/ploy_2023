import 'package:flutter/material.dart';
import 'cora_info.dart';


class muestra_cora extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return MaterialApp(
        home: Scaffold(
            body: cora_info()
        )
    );
  }

}
